services:
  - type: worker
    name: bot-monitor
    env: python
    plan: free
    startCommand: python bot_monitor.py
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: CHAT_ID
        sync: false
